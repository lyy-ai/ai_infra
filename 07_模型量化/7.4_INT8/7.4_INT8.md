# 7.4 INT8 Kernel：INT8 GEMM、Tensor Core 与 Dequant 开销分析

## 1. 什么是 INT8 Kernel

**INT8 Kernel** 是指以 8-bit 整数为核心数据类型的计算核函数，主要用于深度学习推理中的矩阵乘法（GEMM）。它是大模型推理加速和降本的关键技术之一。

### 核心目标

- **提升计算吞吐**：现代 GPU 的 INT8 峰值算力通常是 FP16 的 2 倍，FP32 的 8 倍以上。
- **降低显存带宽**：INT8 数据量是 FP16 的 1/2、FP32 的 1/4，访存压力大幅降低。
- **降低显存占用**：权重和激活用 INT8 存储，可部署更大模型或更长上下文。

### 核心思想

把浮点矩阵乘法：

$$C_{FP32} = A_{FP32} \times B_{FP32}$$

转换为：

$$C_{FP32} = s_A \cdot s_B \cdot (A_{INT8} \times B_{INT8})$$

其中 $A_{INT8}$ 和 $B_{INT8}$ 是量化后的 8-bit 整数矩阵，$s_A$、$s_B$ 是对应的 scale。乘法结果在 INT32 累加器中累加，最后通过 dequantization 还原为浮点。

---

## 2. INT8 GEMM 数学定义

### 2.1 量化输入矩阵

设浮点矩阵 $A \in \mathbb{R}^{M \times K}$，按 per-tensor 对称量化：

$$A_{INT8} = \mathrm{round}\Big(\frac{A}{s_A}\Big), \quad s_A = \frac{\max(|A|)}{127}$$

同理对 $B \in \mathbb{R}^{K \times N}$：

$$B_{INT8} = \mathrm{round}\Big(\frac{B}{s_B}\Big), \quad s_B = \frac{\max(|B|)}{127}$$

### 2.2 INT8 GEMM 计算

INT8 矩阵乘法在硬件上通常按 INT32 累加：

$$C_{INT32} = A_{INT8} \times B_{INT8}$$

注意：$A_{INT8} \in [-128, 127]$，$B_{INT8} \in [-128, 127]$，乘积范围为 $[-16256, 16256]$，累加 K 次后需要用 INT32 保存，避免溢出。

### 2.3 反量化输出

$$C_{FP32} = s_A \cdot s_B \cdot C_{INT32}$$

这就是最常见的 **INT8 GEMM + FP32 输出** 流程。如果下一层也需要 INT8 输入，则还需要做一次 re-quantization：

$$C_{INT8} = \mathrm{round}\Big(\frac{s_A \cdot s_B \cdot C_{INT32}}{s_C}\Big)$$

其中 $s_C$ 是输出张量的 scale。

### 2.4 为什么 INT8 能加速

1. **硬件并行度**：8-bit ALU 可以同时处理更多数据。
2. **Tensor Core**：NVIDIA Tensor Core 一条指令可以完成 $4\times4\times8$ 或更大的 INT8 矩阵乘累加。
3. **访存带宽减半**：同样数据量只需要 FP16 一半的内存带宽。

---

## 3. INT8 GEMM 实现

### 3.1 基础实现思路

一个最简单的 INT8 GEMM 可以写成三层循环：

```python
for m in range(M):
    for n in range(N):
        acc = 0
        for k in range(K):
            acc += int(A[m, k]) * int(B[k, n])
        C[m, n] = acc
```

真实的高性能实现会加入：

- **分块（Tiling）**：把大矩阵切分成适合 L1/L2/Shared Memory 的小块。
- **向量化**：SIMD / CUDA warp 一次处理多个元素。
- **Tensor Core 调用**：wmma/mma.sync 指令。
- **双缓冲 / 流水线**：隐藏访存延迟。
- **融合 Dequant**：把反量化直接写进同一个 kernel，减少 kernel launch 和数据搬运。

### 3.2 融合 vs 非融合 Dequant

| 方式 | 流程 | 优点 | 缺点 |
|------|------|------|------|
| 非融合 | `INT8 GEMM → INT32 → Dequant kernel → FP32` | 逻辑清晰 | 多一次 kernel launch 和内存读写 |
| 融合 | `INT8 GEMM → INT32 → Dequant in same kernel → FP32` | 省一次全局内存读写 | 实现复杂，需要手写 CUDA |

---

## 4. Tensor Core INT8 支持

### 4.1 什么是 Tensor Core

**Tensor Core** 是 NVIDIA GPU 中专门用于矩阵运算的加速单元。与传统 CUDA Core 一次只能做 1 个 FMA（乘加）不同，Tensor Core 可以一次性完成一个小型矩阵乘累加（如 $4\times4\times4$、$8\times8\times4$、$16\times8\times16$）。

### 4.2 支持 INT8 的 GPU 架构

| 架构 | 代表显卡 | INT8 Tensor Core | 峰值 INT8 TOPS |
|------|---------|-----------------|----------------|
| Volta | V100 | 不支持 | - |
| Turing | RTX 2080 Ti, T4 | 支持 | ~130 TOPS |
| Ampere | RTX 3090, A100 | 支持 | ~312 TOPS (A100) |
| Ada Lovelace | RTX 4090, L4 | 支持 | ~330 TOPS |
| Hopper | H100, H200 | 支持 | ~3958 TOPS (H100) |

> 注：INT8 算力通常指稀疏/结构化峰值，实际 dense 算力约为 1/2。

### 4.3 Tensor Core 的 INT8 数据排布

Tensor Core 要求输入矩阵按特定方式分块（如 NVIDIA 的 `wmma` 需要 `mem_row_major` 或 `mem_col_major`）。常见的 INT8 模式：

- **A 矩阵**：INT8，$M \times K$
- **B 矩阵**：INT8，$K \times N$
- **累加器**：INT32，$M \times N$

### 4.4 在 PyTorch 中使用 INT8 计算

PyTorch 目前不直接暴露 Tensor Core 的 INT8 matmul 接口到 Python 层，但可以通过以下方式调用：

1. **torch.matmul with int8 + int32 accumulation**（在支持的设备上底层会调用 Tensor Core）
2. **torch._int_mm**（PyTorch 2.0+ 提供的 INT8 矩阵乘）
3. **TensorRT / ONNX Runtime / CUTLASS**（生产环境推荐）

---

## 5. Dequant 开销分析

### 5.1 Dequant 发生在哪里

在 INT8 推理中，dequantization 通常出现在：

1. **线性层输出后**：把 INT32 累加结果还原成 FP16/FP32，送入下一层。
2. **attention 后**：softmax、layernorm 等算子通常需要浮点精度。
3. **最终输出前**：把 logits 从 INT8/FP16 还原为浮点。

### 5.2 Dequant 的三种粒度

| 粒度 | 计算方式 | 开销 | 精度 |
|------|---------|------|------|
| per-tensor | 一个 scale | 最小 | 最低 |
| per-channel | 每输出通道一个 scale | 中等 | 高 |
| per-token | 每序列位置一个 scale | 较大 | 最高（对激活常用） |

### 5.3 Dequant 开销来源

1. **额外计算**：每个输出元素至少一次乘法和一次类型转换。
2. **额外内存读写**：INT32 累加器结果很大，dequant 需要读一遍再写一遍。
3. **kernel launch 开销**：单独的 dequant kernel 会增加启动延迟。
4. **融合收益**：把 dequant 融进 GEMM kernel 可以省一次全局内存写回。

### 5.4 典型开销比例

在一个 INT8 线性层中，如果 dequant 和 GEMM 分开：

- INT8 GEMM 计算：约 40% 时间
- INT32 累加与 dequant：约 30% 时间
- 内存读写与 kernel launch：约 30% 时间

通过融合优化，后两项可以大幅降低。

---

## 6. 代码示例

### 6.1 INT8 GEMM 基础实现：`int8_gemm.py`

```python
# int8_gemm.py
import torch
import torch.nn as nn
import time


def quantize_symmetric(x, num_bits=8):
    """对称量化到 int8"""
    qmax = 2 ** (num_bits - 1) - 1
    scale = x.abs().max() / qmax
    scale = torch.where(scale == 0, torch.ones_like(scale), scale)
    x_q = torch.round(x / scale).clamp(-qmax - 1, qmax).to(torch.int8)
    return x_q, scale


def int8_gemm_naive(a_fp32, b_fp32):
    """
    手动实现 INT8 GEMM：
    1. 量化 A 和 B
    2. INT8 矩阵乘，累加用 INT32
    3. 反量化为 FP32
    """
    a_q, s_a = quantize_symmetric(a_fp32)
    b_q, s_b = quantize_symmetric(b_fp32)

    # INT8 矩阵乘，结果用 INT32 累加
    c_q = torch.matmul(a_q.to(torch.int32), b_q.to(torch.int32))

    # 反量化
    c_fp32 = s_a * s_b * c_q.to(torch.float32)
    return c_fp32


def benchmark_int8_gemm(M, K, N, iterations=100):
    """对比 FP32、FP16、INT8 GEMM 的精度和速度"""
    torch.manual_seed(42)
    a_fp32 = torch.randn(M, K)
    b_fp32 = torch.randn(K, N)

    # FP32 参考
    c_fp32 = torch.matmul(a_fp32, b_fp32)

    # FP16 参考
    a_fp16 = a_fp32.half()
    b_fp16 = b_fp32.half()
    c_fp16 = torch.matmul(a_fp16, b_fp16).float()

    # INT8 实现
    c_int8 = int8_gemm_naive(a_fp32, b_fp32)

    # 精度对比
    mse_fp16 = torch.mean((c_fp32 - c_fp16) ** 2).item()
    mse_int8 = torch.mean((c_fp32 - c_int8) ** 2).item()
    print(f"M={M}, K={K}, N={N}")
    print(f"  FP16 MSE: {mse_fp16:.6f}")
    print(f"  INT8 MSE: {mse_int8:.6f}")

    # 速度对比（粗略，CPU 上不一定能体现加速）
    if torch.cuda.is_available():
        device = torch.device('cuda')
        a_fp32_d = a_fp32.to(device)
        b_fp32_d = b_fp32.to(device)
        a_fp16_d = a_fp32_d.half()
        b_fp16_d = b_fp32_d.half()

        # 预热
        for _ in range(10):
            torch.matmul(a_fp32_d, b_fp32_d)
            torch.matmul(a_fp16_d, b_fp16_d)

        torch.cuda.synchronize()
        start = time.time()
        for _ in range(iterations):
            torch.matmul(a_fp32_d, b_fp32_d)
        torch.cuda.synchronize()
        fp32_time = time.time() - start

        start = time.time()
        for _ in range(iterations):
            torch.matmul(a_fp16_d, b_fp16_d)
        torch.cuda.synchronize()
        fp16_time = time.time() - start

        print(f"  FP32 time: {fp32_time * 1000:.2f} ms")
        print(f"  FP16 time: {fp16_time * 1000:.2f} ms")
        print(f"  FP16 speedup: {fp32_time / fp16_time:.2f}x")
    else:
        print("  CUDA not available, skipping GPU benchmark")


if __name__ == "__main__":
    benchmark_int8_gemm(256, 512, 256)
```

### 6.2 PyTorch INT8 矩阵乘与 Tensor Core：`tensor_core_int8.py`

```python
# tensor_core_int8.py
import torch
import time


def check_tensor_core_support():
    """检查当前 GPU 是否支持 INT8 Tensor Core"""
    if not torch.cuda.is_available():
        print("CUDA not available, cannot test Tensor Core INT8")
        return False

    device = torch.device('cuda')
    prop = torch.cuda.get_device_properties(device)
    print(f"GPU: {prop.name}")
    print(f"Compute Capability: {prop.major}.{prop.minor}")

    # Turing 及以上架构支持 INT8 Tensor Core
    if prop.major >= 7:
        print("INT8 Tensor Core: supported")
        return True
    else:
        print("INT8 Tensor Core: NOT supported")
        return False


def demo_int8_matmul():
    """使用 PyTorch 的 int8 矩阵乘"""
    torch.manual_seed(42)
    M, K, N = 256, 512, 256

    if hasattr(torch, '_int_mm') and torch.cuda.is_available():
        a = torch.randint(-128, 127, (M, K), dtype=torch.int8, device='cuda')
        b = torch.randint(-128, 127, (K, N), dtype=torch.int8, device='cuda')
        c = torch._int_mm(a, b)
        print(f"Using torch._int_mm on CUDA")
    else:
        # PyTorch 1.10 没有 torch._int_mm，退到 CPU 用 int32 累加演示
        a = torch.randint(-128, 127, (M, K), dtype=torch.int8)
        b = torch.randint(-128, 127, (K, N), dtype=torch.int8)
        c = torch.matmul(a.to(torch.int32), b.to(torch.int32))
        print(f"torch._int_mm not available, using CPU int32 matmul fallback")

    print(f"INT8 x INT8 -> INT32 result shape: {c.shape}")
    print(f"Result dtype: {c.dtype}")
    print(f"Sample values: {c[0, :5]}")


def benchmark_int8_vs_fp16(M, K, N, iterations=100):
    """粗略对比 INT8 与 FP16 矩阵乘耗时"""
    if not torch.cuda.is_available():
        print("CUDA not available, skipping GPU benchmark")
        return

    if not hasattr(torch, '_int_mm'):
        print("torch._int_mm not available in this PyTorch version, skipping INT8 speedup benchmark")
        print("  (In production, use TensorRT / CUTLASS / newer PyTorch for INT8 Tensor Core GEMM)")
        return

    a_i8 = torch.randint(-128, 127, (M, K), dtype=torch.int8, device='cuda')
    b_i8 = torch.randint(-128, 127, (K, N), dtype=torch.int8, device='cuda')
    a_f16 = torch.randn(M, K, dtype=torch.float16, device='cuda')
    b_f16 = torch.randn(K, N, dtype=torch.float16, device='cuda')

    # 预热
    for _ in range(10):
        torch._int_mm(a_i8, b_i8)
        torch.matmul(a_f16, b_f16)

    torch.cuda.synchronize()
    start = time.time()
    for _ in range(iterations):
        torch._int_mm(a_i8, b_i8)
    torch.cuda.synchronize()
    int8_time = time.time() - start

    start = time.time()
    for _ in range(iterations):
        torch.matmul(a_f16, b_f16)
    torch.cuda.synchronize()
    fp16_time = time.time() - start

    print(f"M={M}, K={K}, N={N}")
    print(f"  INT8 time: {int8_time * 1000:.2f} ms")
    print(f"  FP16 time: {fp16_time * 1000:.2f} ms")
    print(f"  Speedup: {fp16_time / int8_time:.2f}x")


if __name__ == "__main__":
    check_tensor_core_support()
    demo_int8_matmul()
    benchmark_int8_vs_fp16(512, 1024, 512)
```

### 6.3 Dequant 开销分析：`dequant_overhead.py`

```python
# dequant_overhead.py
import torch
import time


def fake_int8_gemm_with_dequant(a, b, s_a, s_b, dequant_granularity='per-tensor', c_int32=None):
    """
    模拟 INT8 GEMM + Dequant 的完整流程。
    a, b: 已经量化好的 int8 矩阵
    s_a, s_b: scale
    c_int32: 预计算的 INT32 GEMM 结果，避免重复计算
    """
    if c_int32 is None:
        c_int32 = torch.matmul(a.to(torch.int32), b.to(torch.int32))

    s_out = s_a * s_b
    M, N = c_int32.shape

    if dequant_granularity == 'per-tensor':
        # 整个张量一个 scale
        c_fp = s_out * c_int32.to(torch.float32)
    elif dequant_granularity == 'per-channel':
        # 每输出通道（每列）一个 scale
        scales = torch.full((1, N), s_out, dtype=torch.float32)
        c_fp = scales * c_int32.to(torch.float32)
    elif dequant_granularity == 'per-token':
        # 每输入 token（每行）一个 scale
        scales = torch.full((M, 1), s_out, dtype=torch.float32)
        c_fp = scales * c_int32.to(torch.float32)
    else:
        raise ValueError(f"Unknown granularity: {dequant_granularity}")

    return c_fp


def benchmark_dequant_overhead(M, K, N, iterations=100):
    """对比不同 dequant 粒度的开销"""
    # PyTorch 1.10 中 CUDA 不支持 int32 matmul，统一在 CPU 上演示
    device = torch.device('cpu')
    print(f"Running on CPU (PyTorch 1.10 CUDA does not support int32 matmul)")

    torch.manual_seed(42)
    a = torch.randint(-128, 127, (M, K), dtype=torch.int8, device=device)
    b = torch.randint(-128, 127, (K, N), dtype=torch.int8, device=device)
    s_a = 0.01
    s_b = 0.02

    # 先预计算 INT32 结果，后续 dequant 都基于它
    c_int32 = torch.matmul(a.to(torch.int32), b.to(torch.int32))

    # 纯 INT8 GEMM（无 dequant）
    start = time.time()
    for _ in range(iterations):
        c = torch.matmul(a.to(torch.int32), b.to(torch.int32))
    gemm_time = time.time() - start

    # 分别测量 dequant 本身的时间
    start = time.time()
    for _ in range(iterations):
        c = fake_int8_gemm_with_dequant(a, b, s_a, s_b, 'per-tensor', c_int32)
    per_tensor_time = time.time() - start

    start = time.time()
    for _ in range(iterations):
        c = fake_int8_gemm_with_dequant(a, b, s_a, s_b, 'per-channel', c_int32)
    per_channel_time = time.time() - start

    start = time.time()
    for _ in range(iterations):
        c = fake_int8_gemm_with_dequant(a, b, s_a, s_b, 'per-token', c_int32)
    per_token_time = time.time() - start

    print(f"M={M}, K={K}, N={N}, iterations={iterations}, device={device}")
    print(f"  Pure INT8 GEMM:       {gemm_time * 1000:.2f} ms")
    print(f"  Dequant per-tensor:   {per_tensor_time * 1000:.2f} ms ({(per_tensor_time/gemm_time)*100:.1f}% of GEMM)")
    print(f"  Dequant per-channel:  {per_channel_time * 1000:.2f} ms ({(per_channel_time/gemm_time)*100:.1f}% of GEMM)")
    print(f"  Dequant per-token:    {per_token_time * 1000:.2f} ms ({(per_token_time/gemm_time)*100:.1f}% of GEMM)")
    print(f"  Note: smaller dequant time means faster; overhead is relative to GEMM time")


if __name__ == "__main__":
    benchmark_dequant_overhead(512, 1024, 512)
```

---

## 7. INT8 GEMM 推理流程图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         INT8 线性层推理流程                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   输入激活 A (FP16/FP32)                                             │
│        │                                                            │
│        ▼                                                            │
│   ┌─────────────────────────────────────────┐                       │
│   │  Quantize: A_fp -> A_int8              │                       │
│   │  scale_A = max(|A|) / 127             │                       │
│   └─────────────────┬───────────────────────┘                       │
│                     │                                               │
│   权重 W (FP16/FP32)                                                 │
│        │                                                            │
│        ▼                                                            │
│   ┌─────────────────────────────────────────┐                       │
│   │  Quantize: W_fp -> W_int8              │                       │
│   │  scale_W = max(|W|) / 127             │                       │
│   └─────────────────┬───────────────────────┘                       │
│                     │                                               │
│                     ▼                                               │
│   ┌─────────────────────────────────────────┐                       │
│   │  INT8 GEMM on Tensor Core              │                       │
│   │  C_int32 = A_int8 @ W_int8             │                       │
│   └─────────────────┬───────────────────────┘                       │
│                     │                                               │
│                     ▼                                               │
│   ┌─────────────────────────────────────────┐                       │
│   │  Dequant / Requant                      │                       │
│   │  C_fp = scale_A * scale_W * C_int32     │                       │
│   │  or C_int8 = round(C_fp / scale_C)      │                       │
│   └─────────────────┬───────────────────────┘                       │
│                     │                                               │
│                     ▼                                               │
│   输出激活 C (FP16/FP32 or INT8)                                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 8. Tensor Core 中的 INT8 运算模式

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Tensor Core INT8 矩阵乘示意图                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   A (INT8)                    B (INT8)                C (INT32)    │
│   ┌─────────┐                 ┌─────────┐           ┌─────────┐    │
│   │  M × K  │    @              │  K × N  │    =      │  M × N  │    │
│   │ 8×8×16  │                 │ 8×16×8  │           │ 8×8×8   │    │
│   └─────────┘                 └─────────┘           └─────────┘    │
│        │                            │                     │         │
│        └────────────┬──────────────┘                     │         │
│                     Tensor Core                         │         │
│                 1 个 mma.sync 指令                       │         │
│                     INT8 × INT8 → INT32                 │         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 9. INT8 Kernel 在 LLM 推理栈中的位置

```
┌─────────────────────────────────────────────────────────────────────┐
│                        LLM 推理部署技术栈                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  上层：vLLM / TGI / TensorRT-LLM / MLC-LLM / llama.cpp             │
│                              │                                      │
│                              ▼                                      │
│  调度层：PagedAttention / Continuous Batching / KV Cache 管理        │
│                              │                                      │
│                              ▼                                      │
│  量化层：W4A16 / W8A16 / QAT / AWQ / GPTQ / SmoothQuant            │
│                              │                                      │
│                              ▼                                      │
│  算子层：INT8 GEMM / FP16 GEMM / INT4 Kernel / Marlin / CUTLASS      │
│                 ↑                                                   │
│           你正在学习这一层                                          │
│                              │                                      │
│                              ▼                                      │
│  硬件层：NVIDIA GPU (Tensor Core) / AMD GPU / Intel CPU / NPU       │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

INT8 Kernel 位于**算子层**，直接决定量化模型在硬件上的实际推理速度和显存效率。

---

## 10. INT8 vs FP16 vs W4A16 对比

| 方案 | 权重量化 | 激活量化 | 计算精度 | 显存占用 | 推理速度 | 适用场景 |
|------|---------|---------|---------|---------|---------|---------|
| FP16 | 16-bit | 16-bit | 高 | 基准 | 基准 | 训练、高质量推理 |
| INT8 | 8-bit | 8-bit | 中 | ~50% | ~2x | 通用推理加速 |
| W8A16 | 8-bit | 16-bit | 较高 | ~60% | ~1.5x | 权重敏感、激活不敏感 |
| **W4A16** | **4-bit** | **16-bit** | **较高** | **~31%** | **1.5x~2x** | **大模型推理主流** |
| W4A8 | 4-bit | 8-bit | 中 | ~25% | 2x+ | 极限压缩 |

---

## 11. 主流 INT8 Kernel 实现工具

| 工具 | 厂商 | 特点 | 链接 |
|------|------|------|------|
| **CUTLASS** | NVIDIA | 高性能 CUDA C++ 模板库，支持 INT8 Tensor Core | https://github.com/NVIDIA/cutlass |
| **cuBLAS** | NVIDIA | CUDA 标准库，提供 `cublasGemmEx` | https://docs.nvidia.com/cuda/cublas |
| **TensorRT-LLM** | NVIDIA | 集成 CUTLASS/cuBLAS，针对 LLM 优化 | https://github.com/NVIDIA/TensorRT-LLM |
| **Marlin** | 社区 | 针对 INT4/INT8 量化 LLM 的 GEMM kernel | https://github.com/IST-DASLab/marlin |
| **llama.cpp** | 社区 | CPU/GPU 端 INT8/INT4 推理 | https://github.com/ggerganov/llama.cpp |
| **vLLM** | 社区 | 服务化部署，底层可调用 INT8 kernel | https://github.com/vllm-project/vllm |
| **TVM / MLC-LLM** | 社区 | 自动调优生成 INT8 kernel | https://github.com/mlc-ai/mlc-llm |

---

## 12. 环境准备与运行

在已有的 `humannerf` conda 环境中运行：

```bash
source /data/liyangyang/miniconda3/bin/activate humannerf
cd /data/liyangyang/ai_infra/INT8

python int8_gemm.py
python tensor_core_int8.py
python dequant_overhead.py
```

> 注：Tensor Core INT8 相关的测试需要 NVIDIA Turing 及以上架构的 GPU。CPU 环境只能跑数值逻辑，无法体现硬件加速。

---

## 13. 本节小结

1. **INT8 GEMM** 是 INT8 推理的核心算子，通过把 FP32/FP16 矩阵乘法转换为 INT8 矩阵乘 + INT32 累加实现加速。
2. **Tensor Core** 从 Turing 架构开始支持 INT8，可以提供数倍于 FP16 的矩阵乘吞吐。
3. **Dequantization** 是 INT8 推理中不可忽视的开销，融合到 GEMM kernel 中可以显著降低延迟和内存带宽。
4. per-tensor / per-channel / per-token 三种 dequant 粒度在精度、开销上有不同权衡。
5. 主流实现依赖 NVIDIA CUTLASS/cuBLAS/TensorRT-LLM，社区也有 llama.cpp、Marlin、vLLM 等方案。

---

## 14. 课后思考

1. 为什么 INT8 矩阵乘法需要用 INT32 做累加？如果用 INT8 累加会出现什么问题？
2. Tensor Core 的 INT8 峰值算力为什么比 FP16 高？实际部署中能否完全达到峰值？
3. Dequant 的 per-channel 和 per-token 哪种更适合 LLM 的激活？为什么？
4. 如果 fuse dequant 进 GEMM kernel，具体能省掉哪些内存操作？
5. 为什么 W4A16 推理时通常仍然需要 INT8 或 FP16 的中间计算？
