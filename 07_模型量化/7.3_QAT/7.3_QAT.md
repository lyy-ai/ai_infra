# 7.3 QAT：量化感知训练

## 1. 什么是 QAT

**QAT（Quantization-Aware Training，量化感知训练）** 是一种在训练过程中就模拟低比特量化误差、让模型主动学习适应量化后权重的技术。

与 **PTQ（Post-Training Quantization，训练后量化）** 不同：

| 对比项 | PTQ | QAT |
|--------|-----|-----|
| 量化时机 | 模型训练完成后直接量化 | 训练/微调过程中插入伪量化节点 |
| 是否需要重新训练 | 不需要 | 需要少量 epoch 微调 |
| 精度损失 | 较大，尤其是低比特 | 更小，接近原始精度 |
| 计算成本 | 低 | 较高（训练成本） |
| 适用场景 | 快速部署、对精度要求不高 | 对精度敏感、低比特部署 |

### 核心目标

- **让模型“知道”自己会被量化**：前向传播时就模拟 INT8 / INT4 量化的离散误差。
- **让网络参数适应量化**：通过反向传播微调，使权重量化后的损失最小。
- **在推理时直接使用低比特权重**，无需额外校准。

### 核心思想

QAT 的关键是在网络中插入 **伪量化节点（Fake Quantize Node）**：

```
输入 x (FP32) → [Fake Quantize] → x_fake (FP32, 但模拟 INT8) → 下一层
权重 W (FP32) → [Fake Quantize] → W_fake (FP32, 但模拟 INT8) → 卷积 / 线性层
```

伪量化节点在前向传播中执行“量化 + 反量化”，让网络一直看到近似量化后的值；反向传播中通过 **STE（Straight-Through Estimator）** 把梯度传回浮点权重。

---

## 2. 数学定义

### 2.1 标准量化函数

给定浮点值 $r$、量化 scale $s$、zero point $z$，量化到 $[q_{\min}, q_{\max}]$：

$$q = \mathrm{clip}\Big(\mathrm{round}\big(\frac{r}{s}\big) + z,\; q_{\min},\; q_{\max}\Big)$$

反量化：

$$\hat{r} = s \cdot (q - z)$$

对于**对称量化**（常见于权重）：$z = 0$，$q_{\min} = -2^{b-1}$，$q_{\max} = 2^{b-1} - 1$。

### 2.2 伪量化节点

伪量化 = 量化 → 反量化，但结果仍用浮点表示：

$$r_{\text{fake}} = s \cdot \Big(\mathrm{clip}\Big(\mathrm{round}\big(\frac{r}{s}\big) + z,\; q_{\min},\; q_{\max}\Big) - z\Big)$$

这相当于把浮点值“四舍五入”到最近的量化网格点上，但类型仍是 FP32。

### 2.3 梯度直通估计（STE）

量化函数中的 `round()` 和 `clip()` 是不可导的。为了让反向传播继续，QAT 使用 **STE**：

**前向传播**：

$$r_{\text{fake}} = \text{QuantizeDequantize}(r)$$

**反向传播**：

$$\frac{\partial r_{\text{fake}}}{\partial r} \approx 1$$

即：把量化函数在反向时近似为恒等函数，梯度直接穿过伪量化节点。

PyTorch 中的经典实现：

```python
return r + (r_fake - r).detach()
```

前向：返回 `r_fake`；反向：梯度直接传给 `r`。

---

## 3. 伪量化节点插入

### 3.1 插入位置

通常插入在以下位置：

1. **权重输入**：卷积层 / 线性层的权重前。
2. **激活输出**：ReLU、SiLU、GELU 等激活函数后。
3. **输入/输出边界**：模型输入和最终输出（有时可选）。

```
输入 → [FakeQuant] → Conv(W) → [FakeQuant] → ReLU → [FakeQuant] → Conv(W) → ...
```

### 3.2 对称量化 vs 非对称量化

| 类型 | zero point | 适用场景 |
|------|-------------|----------|
| 对称量化 | $z = 0$ | 权重，通常分布关于 0 对称 |
| 非对称量化 | $z \neq 0$ | 激活，分布可能整体偏移 |

### 3.3 per-tensor vs per-channel

| 粒度 | 说明 | 精度 | 额外开销 |
|------|------|------|----------|
| per-tensor | 整个张量共用一组 scale/z | 一般 | 小 |
| per-channel | 每个输出通道单独 scale | 更高 | 略大 |

对大模型权重通常使用 **per-channel symmetric** 量化，激活通常使用 **per-tensor asymmetric**。

---

## 4. STE 为什么重要

### 4.1 没有 STE 会怎样

伪量化节点的 `round()` 操作梯度几乎处处为 0：

```
round(1.2) = 1, round(1.3) = 1, round(1.4) = 1
```

这些“阶梯”导致梯度无法传播，模型无法学习。

### 4.2 STE 的直观理解

把“阶梯函数”在反向时近似成一条斜线：

```
前向：  round(x)  阶梯状
反向：  d/dx ≈ 1  直线
```

这样网络可以在量化网格的“阶梯”上继续微调，寻找更鲁棒的量化后权重。

### 4.3 STE 的局限性

- 是一种近似，不是真实梯度。
- 对于极低比特（如 INT2、INT1）可能不够稳定。
- 实践中通常需要配合学习率衰减、更长时间的微调。

---

## 5. QAT 训练流程与调优

### 5.1 标准流程

```
步骤 1：准备一个预训练好的 FP32/FP16 模型
步骤 2：在模型中插入伪量化节点（权重 + 激活）
步骤 3：冻结 BN 统计量（可选，视框架而定）
步骤 4：用较小的学习率继续训练若干 epoch
步骤 5：收敛后，导出为真正的 INT8/INT4 量化模型
步骤 6：在目标推理框架上部署
```

### 5.2 关键调参经验

| 参数 | 建议 | 原因 |
|------|------|------|
| 学习率 | 原始训练 LR 的 1/10 ~ 1/100 | 避免破坏已收敛的浮点权重 |
| 训练 epoch | 5 ~ 30 | 让网络适应量化误差 |
| 优化器 | AdamW / SGD + momentum | 通常沿用原配置 |
| BN 层 | 冻结 running statistics | 量化会改变激活分布 |
| 权重衰减 | 可减小或保持 | 避免过度正则化 |
| 量化范围 | 使用 moving average | 稳定激活 scale 估计 |

### 5.3 常见训练策略

1. **QAT from scratch**：随机初始化后全程插入伪量化节点训练，效果通常最好但成本高。
2. **PTQ + QAT fine-tuning**：先训练浮点模型，再插入伪量化节点微调，工业界最常用。
3. **Layer-wise QAT**：逐层微调，适合超大模型。

---

## 6. 代码示例

### 6.1 伪量化节点实现：`qat_fake_quantize.py`

```python
# qat_fake_quantize.py
import torch
import torch.nn as nn


class FakeQuantize(nn.Module):
    """
    伪量化节点：前向模拟量化-反量化，反向使用 STE。
    支持对称 / 非对称、per-tensor / per-channel。
    """
    def __init__(self, num_bits=8, symmetric=True, per_channel=False):
        super().__init__()
        self.num_bits = num_bits
        self.symmetric = symmetric
        self.per_channel = per_channel

        if symmetric:
            self.qmin = -(2 ** (num_bits - 1))
            self.qmax = 2 ** (num_bits - 1) - 1
        else:
            self.qmin = 0
            self.qmax = 2 ** num_bits - 1

    def _compute_scale_zp(self, x):
        """计算 scale 和 zero point"""
        if self.symmetric:
            if self.per_channel:
                max_abs = x.abs().max(dim=-1, keepdim=True).values
            else:
                max_abs = x.abs().max()
            scale = max_abs / (2 ** (self.num_bits - 1) - 1)
            scale = torch.where(scale == 0, torch.ones_like(scale), scale)
            zp = torch.zeros_like(scale)
        else:
            if self.per_channel:
                x_min = x.min(dim=-1, keepdim=True).values
                x_max = x.max(dim=-1, keepdim=True).values
            else:
                x_min = x.min()
                x_max = x.max()
            scale = (x_max - x_min) / (self.qmax - self.qmin)
            scale = torch.where(scale == 0, torch.ones_like(scale), scale)
            zp = torch.round(-x_min / scale).clamp(self.qmin, self.qmax)
        return scale, zp

    def forward(self, x):
        scale, zp = self._compute_scale_zp(x)
        x_int = torch.round(x / scale + zp).clamp(self.qmin, self.qmax)
        x_deq = scale * (x_int - zp)
        # STE：前向返回反量化值，反向梯度直接传给 x
        return x + (x_deq - x).detach()


def test_fake_quantize():
    torch.manual_seed(42)
    fq = FakeQuantize(num_bits=8, symmetric=True)
    x = torch.randn(4, 8, requires_grad=True)
    y = fq(x)
    loss = y.sum()
    loss.backward()

    print("Input shape:", x.shape)
    print("Output shape:", y.shape)
    print("Gradient is all ones:", x.grad.abs().allclose(torch.ones_like(x.grad)))
    print("Original input sample:", x.detach()[0][:5].tolist())
    print("Fake-quantized output sample:", y.detach()[0][:5].tolist())

    # 8-bit 量化后，输出值应该落在量化网格上
    print("\nMSE between input and fake-quantized:", torch.mean((x.detach() - y.detach()) ** 2).item())


if __name__ == "__main__":
    test_fake_quantize()
```

### 6.2 STE 机制可视化：`qat_ste.py`

```python
# qat_ste.py
import torch
import torch.nn as nn
import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt


class STEQuantize(torch.autograd.Function):
    """自定义 autograd 函数，显式实现 STE"""

    @staticmethod
    def forward(ctx, x, scale, qmin, qmax):
        x_int = torch.round(x / scale).clamp(qmin, qmax)
        x_deq = x_int * scale
        ctx.save_for_backward(torch.ones_like(x))
        return x_deq

    @staticmethod
    def backward(ctx, grad_output):
        # STE：梯度直接穿过
        return grad_output, None, None, None


def visualize_ste():
    """可视化 STE 前向阶梯 vs 反向直通"""
    x = torch.linspace(-3, 3, 100, requires_grad=True)
    scale = 0.5
    qmin, qmax = -3, 3

    y = STEQuantize.apply(x, scale, qmin, qmax)
    y.sum().backward()

    plt.figure(figsize=(10, 4))

    plt.subplot(1, 2, 1)
    plt.plot(x.detach().numpy(), y.detach().numpy())
    plt.title("Forward: staircase (round + clip)")
    plt.xlabel("input x")
    plt.ylabel("fake_quant(x)")
    plt.grid(True)

    plt.subplot(1, 2, 2)
    plt.plot(x.detach().numpy(), x.grad.numpy())
    plt.title("Backward: straight-through (gradient ≈ 1)")
    plt.xlabel("input x")
    plt.ylabel("gradient")
    plt.grid(True)

    plt.tight_layout()
    plt.savefig("ste_visualize.png")
    print("Saved STE visualization to ste_visualize.png")


def test_ste_gradient():
    """验证 STE 确实把梯度传回了输入"""
    x = torch.randn(4, 8, requires_grad=True)
    scale = 0.1
    qmin, qmax = -7, 7

    y = STEQuantize.apply(x, scale, qmin, qmax)
    loss = (y ** 2).sum()
    loss.backward()

    print("Input gradient norm:", x.grad.norm().item())
    print("Gradient is non-zero:", x.grad.abs().sum().item() > 0)


if __name__ == "__main__":
    test_ste_gradient()
    visualize_ste()
```

### 6.3 QAT 训练流程：`qat_training_flow.py`

```python
# qat_training_flow.py
import torch
import torch.nn as nn
import torch.optim as optim
from torchvision import datasets, transforms
import os


class FakeQuantize(nn.Module):
    """简化版伪量化节点"""
    def __init__(self, num_bits=8):
        super().__init__()
        self.num_bits = num_bits
        self.qmin = -(2 ** (num_bits - 1))
        self.qmax = 2 ** (num_bits - 1) - 1

    def forward(self, x):
        scale = x.abs().max() / (2 ** (self.num_bits - 1) - 1)
        scale = torch.where(scale == 0, torch.ones_like(scale), scale)
        x_int = torch.round(x / scale).clamp(self.qmin, self.qmax)
        x_deq = x_int * scale
        return x + (x_deq - x).detach()


class QATLinear(nn.Module):
    """带伪量化的线性层"""
    def __init__(self, in_features, out_features, num_bits=8):
        super().__init__()
        self.weight = nn.Parameter(torch.randn(out_features, in_features) * 0.1)
        self.bias = nn.Parameter(torch.zeros(out_features))
        self.weight_fake_quant = FakeQuantize(num_bits)
        self.act_fake_quant = FakeQuantize(num_bits)

    def forward(self, x):
        w_q = self.weight_fake_quant(self.weight)
        x_q = self.act_fake_quant(x)
        return torch.nn.functional.linear(x_q, w_q, self.bias)


class TinyNet(nn.Module):
    def __init__(self, num_bits=8):
        super().__init__()
        self.fc1 = QATLinear(784, 128, num_bits)
        self.relu = nn.ReLU()
        self.fc2 = QATLinear(128, 10, num_bits)

    def forward(self, x):
        x = x.view(x.size(0), -1)
        x = self.relu(self.fc1(x))
        x = self.fc2(x)
        return x


def get_dataloader(batch_size=64, train=True):
    transform = transforms.Compose([
        transforms.ToTensor(),
        transforms.Normalize((0.1307,), (0.3081,))
    ])
    dataset = datasets.MNIST('/tmp/mnist', train=train, download=True, transform=transform)
    return torch.utils.data.DataLoader(dataset, batch_size=batch_size, shuffle=train)


def train_one_epoch(model, dataloader, optimizer, criterion, device):
    model.train()
    total_loss = 0
    for batch_idx, (data, target) in enumerate(dataloader):
        data, target = data.to(device), target.to(device)
        optimizer.zero_grad()
        output = model(data)
        loss = criterion(output, target)
        loss.backward()
        optimizer.step()
        total_loss += loss.item()
        if batch_idx >= 30:  # 每个 epoch 只跑 30 batch，演示用
            break
    return total_loss / (batch_idx + 1)


def evaluate(model, dataloader, device):
    model.eval()
    correct = 0
    total = 0
    with torch.no_grad():
        for data, target in dataloader:
            data, target = data.to(device), target.to(device)
            output = model(data)
            pred = output.argmax(dim=1)
            correct += (pred == target).sum().item()
            total += target.size(0)
            if total >= 1000:
                break
    return correct / total


def demo_qat_training():
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    print(f"Using device: {device}")

    model = TinyNet(num_bits=8).to(device)
    criterion = nn.CrossEntropyLoss()
    optimizer = optim.Adam(model.parameters(), lr=1e-3)

    train_loader = get_dataloader(train=True)
    test_loader = get_dataloader(train=False)

    print("Before QAT fine-tuning:")
    acc_before = evaluate(model, test_loader, device)
    print(f"  Test accuracy: {acc_before:.4f}")

    for epoch in range(3):
        loss = train_one_epoch(model, train_loader, optimizer, criterion, device)
        acc = evaluate(model, test_loader, device)
        print(f"Epoch {epoch+1}: loss={loss:.4f}, test_acc={acc:.4f}")

    print("\nQAT training completed. Model is now adapted to 8-bit quantization.")


if __name__ == "__main__":
    demo_qat_training()
```

### 6.4 QAT 模型集成示例：`qat_model_demo.py`

```python
# qat_model_demo.py
import torch
import torch.nn as nn


class FakeQuantize(nn.Module):
    def __init__(self, num_bits=8):
        super().__init__()
        self.num_bits = num_bits
        self.qmin = -(2 ** (num_bits - 1))
        self.qmax = 2 ** (num_bits - 1) - 1

    def forward(self, x):
        scale = x.abs().max() / (2 ** (self.num_bits - 1) - 1)
        scale = torch.where(scale == 0, torch.ones_like(scale), scale)
        x_int = torch.round(x / scale).clamp(self.qmin, self.qmax)
        x_deq = x_int * scale
        return x + (x_deq - x).detach()


class QATLinear(nn.Module):
    """可替换 nn.Linear 的 QAT 版本"""
    def __init__(self, in_features, out_features, num_bits=8):
        super().__init__()
        self.linear = nn.Linear(in_features, out_features, bias=True)
        self.weight_quant = FakeQuantize(num_bits)
        self.act_quant = FakeQuantize(num_bits)

    def forward(self, x):
        w = self.weight_quant(self.linear.weight)
        x = self.act_quant(x)
        return torch.nn.functional.linear(x, w, self.linear.bias)


class QATMLP(nn.Module):
    """一个 QAT 版本的 MLP，类似 LLM 中的 FFN"""
    def __init__(self, hidden_size=512, intermediate_size=1024, num_bits=8):
        super().__init__()
        self.gate_proj = QATLinear(hidden_size, intermediate_size, num_bits)
        self.up_proj = QATLinear(hidden_size, intermediate_size, num_bits)
        self.down_proj = QATLinear(intermediate_size, hidden_size, num_bits)
        self.act = nn.SiLU()

    def forward(self, x):
        gate = self.act(self.gate_proj(x))
        up = self.up_proj(x)
        return self.down_proj(gate * up)


def replace_linear_with_qat(model, num_bits=8):
    """递归把模型中所有 nn.Linear 替换成 QATLinear"""
    for name, module in model.named_children():
        if isinstance(module, nn.Linear):
            qat_module = QATLinear(module.in_features, module.out_features, num_bits)
            qat_module.linear.weight.data = module.weight.data.clone()
            if module.bias is not None:
                qat_module.linear.bias.data = module.bias.data.clone()
            setattr(model, name, qat_module)
        else:
            replace_linear_with_qat(module, num_bits)


def demo_qat_model():
    torch.manual_seed(0)
    mlp = QATMLP(hidden_size=256, intermediate_size=512, num_bits=8)
    x = torch.randn(2, 10, 256)
    y = mlp(x)
    print(f"Input shape:  {x.shape}")
    print(f"Output shape: {y.shape}")

    total_params = sum(p.numel() for p in mlp.parameters())
    print(f"Total parameters: {total_params}")

    # 模拟 QAT 训练一步
    y.sum().backward()
    print("Backward pass succeeded, all Linear layers have gradients:")
    for name, p in mlp.named_parameters():
        if p.grad is not None:
            print(f"  {name}: grad_norm={p.grad.norm().item():.4f}")


if __name__ == "__main__":
    demo_qat_model()
```

---

## 7. QAT 训练流程图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         QAT 训练流程                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   1. 加载预训练 FP32/FP16 模型                                       │
│        │                                                            │
│        ▼                                                            │
│   2. 插入伪量化节点（权重 + 激活）                                    │
│        │                                                            │
│        ▼                                                            │
│   3. 前向传播：权重/激活先量化 → 反量化 → 参与计算                    │
│        │                                                            │
│        ▼                                                            │
│   4. 计算损失 Loss                                                   │
│        │                                                            │
│        ▼                                                            │
│   5. 反向传播：通过 STE 把梯度传回浮点权重                            │
│        │                                                            │
│        ▼                                                            │
│   6. 优化器更新浮点权重                                              │
│        │                                                            │
│        ▼                                                            │
│   7. 重复 3-6 步，直到收敛                                          │
│        │                                                            │
│        ▼                                                            │
│   8. 导出：将浮点权重转换为真正的 INT8/INT4 量化权重                  │
│        │                                                            │
│        ▼                                                            │
│   9. 部署到 TensorRT / ONNX Runtime / TFLite 等推理引擎              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 8. PTQ vs QAT 流程对比

```
PTQ 流程：
┌──────────┐    ┌────────────┐    ┌─────────────┐    ┌──────────┐
│ 训练模型  │ → │ 校准数据集  │ → │ 统计 scale  │ → │ 量化部署  │
└──────────┘    └────────────┘    └─────────────┘    └──────────┘

QAT 流程：
┌──────────┐    ┌──────────────┐    ┌─────────────┐    ┌──────────┐
│ 训练模型  │ → │ 插入伪量化节点 │ → │ 微调训练     │ → │ 量化部署  │
└──────────┘    └──────────────┘    └─────────────┘    └──────────┘
```

---

## 9. QAT 在 LLM 推理栈中的位置

```
┌─────────────────────────────────────────────────────────────────────┐
│                        LLM 推理部署技术栈                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  上层：vLLM / TGI / TensorRT-LLM / MLC-LLM / llama.cpp             │
│                              │                                      │
│                              ▼                                      │
│  调度层：PagedAttention / Continuous Batching / KV Cache 管理        │
│                              │                                      │
│                              ▼                                      │
│  量化层：PTQ / QAT / W4A16 / W8A16 / AWQ / GPTQ / SmoothQuant        │
│                              │                                      │
│                              ▼                                      │
│  算子层：INT8 GEMM / FP16 GEMM / INT4 Kernel / Marlin / CUTLASS      │
│                              │                                      │
│                              ▼                                      │
│  硬件层：NVIDIA GPU / AMD GPU / Intel CPU / NPU                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

QAT 位于**量化层**，与 PTQ 并列，但更强调“训练阶段的精度恢复”。

---

## 10. QAT vs 其他量化方案

| 方案 | 是否需要训练 | 精度恢复能力 | 部署复杂度 | 适用场景 |
|------|------------|-------------|-----------|---------|
| FP16 | 否 | 无损 | 低 | 训练、高质推理 |
| PTQ INT8 | 否 | 中等 | 低 | 通用推理 |
| **QAT INT8** | **是** | **高** | **中** | **对精度敏感** |
| W4A16 (PTQ) | 否 | 中-高 | 中 | 大模型推理主流 |
| QAT W4A16 | 是 | 高 | 高 | 极限压缩 + 高精度 |
| GPTQ / AWQ | 否 | 高（PTQ 中的佼佼者） | 中 | 大模型 4-bit 部署 |

---

## 11. 主流 QAT 实现工具

| 工具 | 框架 | 特点 | 链接 |
|------|------|------|------|
| **PyTorch FX Graph Mode Quantization** | PyTorch | 官方推荐，支持 QAT 全图优化 | https://pytorch.org/docs/stable/quantization.html |
| **PyTorch Eager Mode Quantization** | PyTorch | 手动插入 Observer + FakeQuantize | https://pytorch.org/tutorials/prototype/graph_mode_dynamic_bert_tutorial.html |
| **TensorFlow QAT** | TensorFlow | Keras 层 API 友好 | https://www.tensorflow.org/model_optimization/guide/quantization/training |
| **ONNX Runtime Quantization** | ONNX | 训练后量化为主，部分支持 QAT | https://onnxruntime.ai/docs/performance/quantization.html |
| **Neural Network Intelligence (NNI)** | 微软 | 综合压缩工具，支持 QAT | https://nni.readthedocs.io/ |
| **FastFold / FlagGems** | 国产 | 面向国产硬件的量化训练方案 | 社区项目 |

---

## 12. 环境准备与运行

在已有的 `humannerf` conda 环境中运行：

```bash
source /data/liyangyang/miniconda3/bin/activate humannerf
cd /data/liyangyang/ai_infra/QAT

python qat_fake_quantize.py
python qat_ste.py
python qat_training_flow.py
python qat_model_demo.py
```

> 注：`qat_training_flow.py` 会下载 MNIST 数据集到 `/tmp/mnist`，并演示一个轻量 QAT 训练流程。

---

## 13. 本节小结

1. **QAT** 是在训练阶段模拟量化误差，让模型主动适应低比特表示。
2. **伪量化节点**在前向执行“量化 → 反量化”，让网络看到量化后的值。
3. **STE（梯度直通估计）** 解决了量化函数不可导的问题，使反向传播能继续更新浮点权重。
4. QAT 通常用于 **PTQ 后精度不足** 的场景，需要少量训练成本。
5. 核心流程：插入伪量化节点 → 微调训练 → 导出真正的量化模型 → 部署。
6. 主流工具包括 PyTorch FX Quantization、TensorFlow QAT、NNI 等。

---

## 14. 课后思考

1. 为什么 QAT 通常比 PTQ 精度更高？代价是什么？
2. STE 把梯度近似为 1，这种近似在哪些情况下会失效？
3. 如果要把 QAT 应用到 LLM 的 Transformer 上，应该在哪些位置插入伪量化节点？
4. 对称量化和非对称量化分别适合权重还是激活？为什么？
