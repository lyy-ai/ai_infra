model: resnet18.onnx
inputs:
  - name: input
    shape: [1, 3, 224, 224]
    dtype: float32

backends:
  - cuda
  - cutlass

output_dir: ./artifacts

passes:
  - InferType
  - FoldConstant
  - FuseOps
  - ConvertLayout:
      nn.conv2d: [NHWC, OHWI]
  - DeadCodeElimination
